# Local COKIT guest SDK

Generic COKIT Tauri guest bindings (sessions, membership, push/resolve, hooks). No messenger/room domain types live here.

Messenger-specific models and ops: `src/lib/messenger/`.

When the official package is published, replace this folder with a dependency on it.
